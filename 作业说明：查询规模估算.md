# 作业说明：查询规模估算

| 任务 | 根据 SQL 查询结构和列统计信息，预测查询结果规模（cardinality） |
| ---- | ------------------------------------------------------------ |
| 平台 | Kaggle 竞赛平台                                              |
| 评测 | MeanQ-error，分数越低越好                                    |

# 1. 作业描述

给定 Kaggle 提供的 SQL 查询结构（Tables、Join Conditions、Predicates）以及列级统计信息，使用机器学习算法估算每条查询的查询规模（Cardinality），即查询执行后预计返回的结果行数。训练集提供真实 Cardinality，测试集只提供查询结构，需要提交预测结果。

**核心任务** 完成特征构造、模型训练与验证、Kaggle 结果提交。

# 2. 推荐工具与环境

| **类别**    | **推荐工具**                   | **说明**                            |
| ----------- | ------------------------------ | ----------------------------------- |
| Python 环境 | Anaconda /Miniconda / venv     | 建议使用独立环境管理依赖            |
| 数据处理    | pandas,numpy                   | 读取CSV、解析字段、构造特征         |
| 机器学习    | scikit-learn,XGBoost, LightGBM | 可用于baseline 与传统监督学习模型   |
| 深度学习    | PyTorch /TensorFlow            | 可用于MLP、set encoder 或更复杂模型 |

# 3. 数据说明

竞赛数据以 CSV 和 SQL schema 文件提供。请以 Kaggle Data 页面中的文件为准。

| **文件**                | **用途**        | **关键字段**                                        | **说明**                               |
| ----------------------- | --------------- | --------------------------------------------------- | -------------------------------------- |
| train.csv               | 训练集          | Id,Tables, Join Conditions, Predicates, Cardinality | 包含标签，可用于训练和本地验证         |
| test.csv                | 测试集          | Id,Tables, Join Conditions, Predicates              | 不包含标签，需要预测Cardinality        |
| sample_submission.csv   | 提交样例        | Id,Cardinality                                      | Kaggle 接受的提交文件格式              |
| column_min_max_vals.csv | 列统计信息      | name, min,max, cardinality, num_unique_values       | 可用于数值归一化、选择率估计和特征构造 |
| schematext.sql          | Schema 辅助信息 | 表结构与列信息                                      | 帮助理解表、列和连接关系               |

## train.csv/test.csv字段含义

| **字段**       | **含义**         |
| -------------- | ---------------- |
| Id             | 查询样本编号     |
| Tables         | 查询涉及的表     |
| JoinConditions | 表之间的连接条件 |
| Predicates     | 过滤谓词         |
| Cardinality    | 查询结果行数     |

# 4. 提交文件格式

Kaggle 提交文件必须包含两列，列名必须为 Id 和 Cardinality，每个测试样本一行。

```
Id,Cardinality
1,82
2,15
3,4
...
```

* 预测值应为正整数；建议对异常的负数、零、浮点数或 NaN 做统一处理后再提交。

* Id 的顺序最好与 sample_submission.csv 保持一致，以减少对齐错误。

# 5. 模型评测

Kaggle 使用 Mean Q-error 评测预测结果。Q-error 衡量预测值与真实值之间的倍数误差。

$QError(y_{pred}, y_{true}) = max(\frac{y_{pred} }{ y_{true}},\frac{y_{true}}{y_{pred}})$
 $Mean QError = \frac1N * \sum_i QError(y_{pred_i}, y_{true_i})$

* 1.0 是理想值，表示预测值完全等于真实 Cardinality。

* 预测 100 vs 真实 10，与预测 10 vs 真实 100 的 Q-error 都是 10。

* Leaderboard 按 Mean Q-error 排名，分数越低越好。
